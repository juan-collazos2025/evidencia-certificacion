git checkout -b feature/certification  # Crea y cambia a la rama correcta
git add .
git commit -m "Subiendo evidencia de certificación"
git push origin feature/certification  # Sube los cambios a GitHub
